{% extends "layouts/layout.html.twig" %}

{% block content %}

    <h1>{{ page_title }}</h1>

    <p>{{ lang.page_support.content|raw }}</p>

    {{ parent() }}

    {{ include("blocks/faq.html.twig") }}

{% endblock %}