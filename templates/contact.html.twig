{% extends "layouts/layout.html.twig" %}

{% block content %}

    <div>
        <h1>{{ site_name }}</h1>

        <table class="table table-responsive">
            <tr>
                <th>{{ lang.page_contact.emailaddress_desc }}</th>
                <td>{{ lang.page_contact.emailaddress }}</td>
            </tr>
            <tr>
                <th>{{ lang.page_contact.phone_desc }}</th>
                <td>{{ lang.page_contact.phone }}</td>
            </tr>
            <tr>
                <th>{{ lang.page_contact.address_desc }}</th>
                <td>{{ lang.page_contact.address }}</td>
            </tr>
            <tr>
                <th>{{lang.page_contact.vat_desc }} </th>
                <td>{{lang.page_contact.vat }}</td>
            </tr>
            <tr>
                <th>{{lang.page_contact.coc_desc }}</th>
                <td>{{ lang.page_contact.coc }}</td>
            </tr>
        </table>
    </div>

{% endblock %}